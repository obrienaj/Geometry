public class PointTester{
    public static void main(String[] args){

        Point p = new Point(1, 1);
        Point q = new Point(2, 1);
        Point r = new Point(3, 3);

        System.out.println("Distance between p and q: " + distance(p, q));
        // given 2 points (x, y), calculate the distance between the points
        // given 3 points (x, y), calculate area of the triangle
    }
}